{
  "name": "planning-with-files",
  "description": "Claude Code fork of planning-with-files: reliable hooks via hooks/hooks.json plus local customizations (per-session binding, 临时任务 suppression, .hooks_mode gating) on top of the official skill, commands, attestation, and PreCompact.",
  "owner": {
    "name": "Ahmad Othman Ammar Adi",
    "url": "https://github.com/OthmanAdi"
  },
  "plugins": [
    {
      "name": "planning-with-files",
      "source": "./",
      "description": "Claude Code fork: reliable hooks via hooks/hooks.json + local customizations (per-session binding, 临时任务 suppression, .hooks_mode gating, dir-based .planning/<id>/ plans with per-session read anchoring; PreToolUse hard-denies a bare resolve-plan-dir.sh run, agent uses injected paths + printed PLAN_ID) on top of the official skill, commands, attestation, and PreCompact.",
      "version": "2.43.0-claude.5"
    }
  ]
}
